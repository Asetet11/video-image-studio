const fs=require('fs');const path=require('path');const srcDir=path.resolve(__dirname,'..','node_modules','@ffmpeg','core','dist');const dstDir=path.resolve(__dirname,'..','public','ffmpeg');if(!fs.existsSync(srcDir)){console.log('WARN: @ffmpeg/core not installed yet. Skipping copy.');process.exit(0)};fs.mkdirSync(dstDir,{recursive:true});for(const f of ['ffmpeg-core.js','ffmpeg-core.wasm','ffmpeg-core.worker.js']){fs.copyFileSync(path.join(srcDir,f),path.join(dstDir,f))}console.log('FFmpeg core copied to public/ffmpeg')
